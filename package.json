{
  "name": "@qrcore/core",
  "version": "0.4.6",
  "description": "Modern QR Code generator with typed ESM, CommonJS, and CLI support.",
  "type": "module",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/summer-illusion/qrcore.git"
  },
  "sideEffects": false,
  "main": "./dist/index.cjs",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.mts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/index.d.mts",
        "default": "./dist/index.mjs"
      },
      "require": {
        "types": "./dist/index.d.cts",
        "default": "./dist/index.cjs"
      }
    }
  },
  "bin": {
    "qrcore": "dist/cli.mjs",
    "qrcode": "dist/cli.mjs"
  },
  "files": [
    "dist"
  ],
  "lint-staged": {
    "*.ts": [
      "oxlint --fix",
      "prettier --write"
    ],
    "*.{mts,cts,js,mjs,json,md,yml,yaml}": [
      "prettier --write"
    ]
  },
  "scripts": {
    "build": "tsdown --config-loader tsx",
    "check": "tsc --noEmit",
    "ci": "npm run lint && npm run check && npm run test && npm run build && npm run smoke && npm run pack:dry",
    "commit-push": "tsx scripts/commit-push.ts",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "changelog": "tsx scripts/generate-changelog.ts",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "pack:dry": "npm pack --dry-run --ignore-scripts",
    "publish:prepare": "tsx scripts/prepare-npm-publish.ts",
    "prepare": "husky",
    "version:bump": "tsx scripts/auto-bump-version.ts",
    "release": "tsx scripts/bump-version.ts",
    "test": "vitest run",
    "smoke": "tsx test/smoke.ts",
    "prepack": "npm run build && npm run test && npm run smoke"
  },
  "dependencies": {
    "cleye": "^2.6.0",
    "dijkstrajs": "^1.0.3",
    "pngjs": "^7.0.0"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.3",
    "@commitlint/config-conventional": "^20.5.3",
    "@types/node": "^24.0.0",
    "husky": "^9.0.0",
    "jsqr": "^1.4.0",
    "lint-staged": "^16.4.0",
    "oxlint": "^1.65.0",
    "prettier": "^3.8.3",
    "tsdown": "^0.22.2",
    "tsx": "^4.22.4",
    "typescript": "~6.0.2",
    "vitest": "^4.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
